export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold">Product Discovery &amp; Definition Tool</h1>
      <p className="max-w-xl text-center text-sm text-neutral-500">
        Scaffold only — see README.md for the build order (Section 44 of the build
        prompt) and what to wire up first: Supabase Auth, then the discovery
        conversation loop.
      </p>
    </main>
  );
}
